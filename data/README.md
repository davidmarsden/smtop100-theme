# Theme data

Place `legacy-authors.json` in this directory.

It is generated from the original WordPress WXR export and contains the recovered historical author lookup used by `layouts/partials/legacy-author.html`.

The lookup key is publication date + exact post title because Micro.blog regenerated slugs during import.
